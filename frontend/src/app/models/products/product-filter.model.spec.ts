import { ProductFilterModel } from './product-filter.model';

describe('ProductFilterModel', () => {
  it('should create an instance', () => {
    expect(new ProductFilterModel()).toBeTruthy();
  });
});
