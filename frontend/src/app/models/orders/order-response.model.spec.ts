import { OrderResponseModel } from './order-response.model';

describe('OrderResponseModel', () => {
  it('should create an instance', () => {
    expect(new OrderResponseModel()).toBeTruthy();
  });
});
