import { PaymentRequestModel } from './payment-request.model';

describe('PaymentRequestModel', () => {
  it('should create an instance', () => {
    expect(new PaymentRequestModel()).toBeTruthy();
  });
});
