import { OrderItemResponseModel } from './order-item-response.model';

describe('OrderItemResponseModel', () => {
  it('should create an instance', () => {
    expect(new OrderItemResponseModel()).toBeTruthy();
  });
});
